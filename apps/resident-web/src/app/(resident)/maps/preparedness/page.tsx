import { PreparednessGuide } from '@/components/maps/preparedness-guide';

// Static content, no per-barangay data — guest-accessible like the rest of Maps.
export default function MapsPreparednessPage() {
  return <PreparednessGuide />;
}

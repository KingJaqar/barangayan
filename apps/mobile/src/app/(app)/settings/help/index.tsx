import { PlaceholderPanel } from '@/components/placeholder-panel';

// Help Center: search + categorized FAQ list (Getting Started, Account & Security,
// Reporting Issues), nested under Settings per the design file — not its own tab.
export default function HelpCenterScreen() {
  return <PlaceholderPanel label="Help Center (search + categorized articles) goes here." />;
}

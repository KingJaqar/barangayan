import { PlaceholderPanel } from '@/components/placeholder-panel';

// DRRM Info Hub → Hotline Directory card. Content (offline-cached hotline list) is a
// later task; the navigation entry point from the Maps screen is real.
export default function HotlineDirectoryScreen() {
  return <PlaceholderPanel label="Hotline directory content goes here." />;
}

import { PlaceholderPanel } from '@/components/placeholder-panel';

// DRRM Info Hub → Preparedness Guide card. Content (offline-cached guide steps) is a
// later task; the navigation entry point from the Maps screen is real.
export default function PreparednessGuideScreen() {
  return <PlaceholderPanel label="Preparedness guide content goes here." />;
}

/**
 * Generated Supabase database types.
 *
 * TODO(supabase): replace this placeholder by running, once the migrations in
 * supabase/migrations/ have been applied to the `barangayan` project:
 *
 *   npx supabase gen types typescript --project-id <project-ref> --schema public > packages/shared/src/types/database.ts
 *
 * Tracked as part of task "Author initial Supabase SQL migrations + RLS policies".
 * Until then, `Database` is left unknown-shaped so consumers don't silently trust a
 * fabricated schema.
 */
export type Database = Record<string, unknown>;
